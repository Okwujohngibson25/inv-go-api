version: "1"
packages:
  - name: "db"
    path: "internal/db"
    queries: "db/queries"
    schema: "db/migrations"
    engine: "postgresql"
    emit_interface: true

    overrides:
      - db_type: "pg_catalog.numeric"
        go_type: "github.com/shopspring/decimal.Decimal"
